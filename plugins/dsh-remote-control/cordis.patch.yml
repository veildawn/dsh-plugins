# Ensure @deepseek-ai/dsh-client-connection has webServer injected so rpc.handle can register HTTP routes
- id: connection
  inject:
    - webRuntime
    - webServer

# Mount the Host-side remote gate with remote access disabled by default.
- insert:
    - id: remote-control
      name: dsh-remote-control
      config:
        enabled: false
