# Ensure @deepseek-ai/dsh-client-connection has webServer injected so rpc.handle can register HTTP routes
- id: connection
  inject:
    - webRuntime
    - webServer

# Cordis bundle patch: mounts the terminal plugin's host half.
- insert:
    - id: terminal
      name: dsh-terminal
