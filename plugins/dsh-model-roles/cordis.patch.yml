# Mount role routing with no forced model assignments. Configure mappings in
# Settings -> 模型角色; an empty table preserves native DSH behavior.
- insert:
    - id: model-roles
      name: dsh-model-roles
      config:
        roles: []
        continuous:
          enabled: true
          maxGoalRounds: 32
        advisor:
          enabled: false
          subagents: false
          provider: spawn
          maxTranscriptChars: 60000
